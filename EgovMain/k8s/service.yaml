apiVersion: v1
kind: Service
metadata:
  name: egov-main
  labels:
    app.kubernetes.io/name: egov-main
    app.kubernetes.io/part-of: egovframe-msa-common
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: egov-main
  ports:
    - name: http
      port: 8080
      targetPort: http
