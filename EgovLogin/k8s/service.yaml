apiVersion: v1
kind: Service
metadata:
  name: egov-login
  labels:
    app.kubernetes.io/name: egov-login
    app.kubernetes.io/part-of: egovframe-msa-common
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: egov-login
  ports:
    - name: http
      port: 8080
      targetPort: http
