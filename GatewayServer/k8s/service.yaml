apiVersion: v1
kind: Service
metadata:
  name: gateway-server
  labels:
    app.kubernetes.io/name: gateway-server
    app.kubernetes.io/part-of: egovframe-msa-common
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway-server
  ports:
    - name: http
      port: 9000
      targetPort: http
