apiVersion: v1
kind: Service
metadata:
  name: egov-author
  labels:
    app.kubernetes.io/name: egov-author
    app.kubernetes.io/part-of: egovframe-msa-common
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: egov-author
  ports:
    - name: http
      port: 8080
      targetPort: http
