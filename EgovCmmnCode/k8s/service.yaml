apiVersion: v1
kind: Service
metadata:
  name: egov-cmmn-code
  labels:
    app.kubernetes.io/name: egov-cmmn-code
    app.kubernetes.io/part-of: egovframe-msa-common
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: egov-cmmn-code
  ports:
    - name: http
      port: 8080
      targetPort: http
